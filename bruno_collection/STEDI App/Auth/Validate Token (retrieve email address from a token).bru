meta {
  name: Validate Token (retrieve email address from a token)
  type: http
  seq: 1
}

get {
  url: {{stediurl}}/validate/{{token}}
  body: none
  auth: none
}

headers {
  suresteps.session.token: {{token}}
}
